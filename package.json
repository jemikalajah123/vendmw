{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch './**/*.ts' --exec 'ts-node' src/index.ts",
    "docker:bash": "docker-compose up -d",
    "docker:up": "docker container start api",
    "docker:down": "docker container stop api"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "dotenv": "^16.0.1",
    "ts-node-dev": "^1.1.8"
  },
  "dependencies": {
    "@types/express": "^4.17.12",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "nodemon": "^2.0.16",
    "pg": "^8.6.0",
    "pine": "^1.1.1",
    "ts-node": "^10.0.0",
    "typeorm": "^0.2.34",
    "typescript": "^4.3.5"
  }
}
